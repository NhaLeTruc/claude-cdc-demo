# PostgreSQL Configuration
POSTGRES_USER=cdcuser
POSTGRES_PASSWORD=cdcpass
POSTGRES_DB=cdcdb
POSTGRES_PORT=5432
POSTGRES_HOST=localhost
POSTGRES_HOST_INTERNAL=postgres

# MySQL Configuration
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_USER=cdcuser
MYSQL_PASSWORD=cdcpass
MYSQL_DB=cdcdb
MYSQL_PORT=3306
MYSQL_HOST=localhost
MYSQL_HOST_INTERNAL=mysql

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:29092
KAFKA_BOOTSTRAP_SERVERS_INTERNAL=kafka:9092
KAFKA_TOPIC_PREFIX=cdc

# Kafka Topics (match Debezium connector topic.prefix configuration)
KAFKA_TOPIC_CUSTOMERS=debezium.public.customers
KAFKA_TOPIC_ORDERS=debezium.public.orders
KAFKA_TOPIC_PRODUCTS=debezium.public.products
KAFKA_TOPIC_INVENTORY=debezium.public.inventory
KAFKA_TOPIC_SCHEMA_EVOLUTION=debezium.public.schema_evo_test

# Default topic for streaming pipelines
KAFKA_TOPIC=debezium.public.customers

# Debezium Configuration
DEBEZIUM_HOST=localhost
DEBEZIUM_HOST_INTERNAL=debezium
DEBEZIUM_PORT=8083
DEBEZIUM_URL=http://localhost:8083

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=localhost:9000
MINIO_BUCKET_DELTALAKE=deltalake
MINIO_BUCKET_ICEBERG=iceberg

# Observability Configuration
PROMETHEUS_PORT=9090
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin
GRAFANA_PORT=3000
LOKI_PORT=3100

# Application Configuration
LOG_LEVEL=INFO
METRICS_PORT=8000
HEALTH_CHECK_PORT=8001

# Data Generation
MOCK_DATA_CUSTOMERS=1000
MOCK_DATA_ORDERS=5000
MOCK_DATA_PRODUCTS=500
MOCK_DATA_INVENTORY=2000

# CDC Configuration
CDC_LAG_THRESHOLD_SECONDS=5
CDC_BATCH_SIZE=100
CDC_POLL_INTERVAL_MS=1000

# Testing
TEST_TIMEOUT_SECONDS=60
TEST_DATA_CLEANUP=true

# Alerting & Notifications Configuration
ALERTMANAGER_PORT=9093
# Webhook endpoint for alert delivery testing (leave empty to skip webhook tests)
ALERT_WEBHOOK_URL=
# Email configuration for alert delivery (leave empty to skip email tests)
ALERT_EMAIL_TO=
ALERT_EMAIL_FROM=
# Slack webhook for alert delivery (leave empty to skip Slack tests)
ALERT_SLACK_WEBHOOK_URL=

# Schema Registry Configuration
SCHEMA_REGISTRY_URL=http://localhost:8081
SCHEMA_REGISTRY_COMPATIBILITY=BACKWARD

# ============================================================================
# Docker Compose Infrastructure Configuration
# ============================================================================

# Kafka Internal Configuration
KAFKA_BROKER_ID=1
KAFKA_INTERNAL_PORT=9092
KAFKA_EXTERNAL_PORT=29092

# Zookeeper Configuration  
ZOOKEEPER_PORT=2181
ZOOKEEPER_TICK_TIME=2000

# MinIO Ports
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# Spark Configuration
SPARK_MASTER_PORT=7077
SPARK_UI_PORT=8080
SPARK_DRIVER_PORT=4040
SPARK_DRIVER_MEMORY=1g
SPARK_EXECUTOR_MEMORY=1g
SPARK_MEMORY_LIMIT=2g
SPARK_CPU_LIMIT=1.0

# Streaming Pipeline Configuration
DELTA_TABLE_PATH=/tmp/delta/customers
ICEBERG_WAREHOUSE=/tmp/iceberg_warehouse
ICEBERG_CATALOG=iceberg
ICEBERG_NAMESPACE=analytics
ICEBERG_TABLE=customers_analytics

# Iceberg REST Catalog
ICEBERG_REST_PORT=8181

# Schema Registry Port
SCHEMA_REGISTRY_PORT=8081
